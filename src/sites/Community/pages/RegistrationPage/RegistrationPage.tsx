import { FC, PropsWithChildren } from 'react';

interface Props extends PropsWithChildren {};

const RegistrationPage: FC<Props> = ({ children }) => {
    return (
        <div className="h-full">
            
        </div>
    )
}

export default RegistrationPage;